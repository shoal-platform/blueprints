# Base URLs of the three services, as reachable from the BROWSER (not the
# container network). All are inlined at build time (NEXT_PUBLIC_*).
NEXT_PUBLIC_ORDERS_API_URL=http://localhost:8081
NEXT_PUBLIC_INVENTORY_API_URL=http://localhost:8082
NEXT_PUBLIC_NOTIFICATIONS_API_URL=http://localhost:8083
